# 开发规则
1. 用户侧的任何提示，必须满足业务语义

# CSS 与组件封装规则

- 禁止脱离具体上下文，把一个孤立的 CSS 样式抽成所谓的“通用样式”并跨页面、跨结构调用。
- 通用必须以一整套稳定的组件上下文为单位：DOM 结构、样式和行为应一起封装在组件内部，再通过明确的组件参数对外使用。
- 只有 DOM 层级、外层容器和布局上下文确定且一致时，样式才具有通用意义。
- 如果两个元素的外层 DOM 层数、父级布局或使用上下文不同，必须各自在所属页面或组件中维护样式，禁止强行共用位置、对齐和视觉校正规则。